#!/bin/bash

BUILD_DIR=$1
CACHE_DIR=$2

echo "Checking files in $BUILD_DIR/scripts/"
ls -la "$BUILD_DIR/scripts/"

echo "Running install submodules script... $BUILD_DIR"
python3 "$BUILD_DIR/scripts/install_submodules.py" "$BUILD_DIR"
